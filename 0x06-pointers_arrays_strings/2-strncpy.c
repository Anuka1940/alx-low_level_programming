char *_strncpy(char *dest, char *src, int n)
{	char *ds = dest;
	char *sc = src;
	char  *_ptr = dest
	int i;

	for (i = 0; i < n && sc[i] != '\0'; i++)
	{	
		ds = sc;
	}
		ds++;
		sc++;'
	{
			ds[i] = '\0';
	}
		return dest;

}

