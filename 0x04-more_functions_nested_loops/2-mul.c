/**
*mul(int a, int b)- multiple two variable a,b
*
*Return: Always return i
*/
int mul(int a, int b)
{
    int i;
    i = a*b;

    if ((a >= 48 && a <= 57) && (b >= 48 && a <= 57))      
 
        return (i);
}