#ifndef BINARY
#define BINARY
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
unsigned int expo(unsigned int base, int power);
unsigned int binary_to_uint(const char *b);
#endif /*(end BINARY) */
